# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# HTTP client (tool calls into the Django backend / analytics service)
httpx==0.28.1

# Environment
python-decouple==3.8

# LLM SDK (agentic tool-calling loop)
openai==1.59.6

# TODO: Add sentry-sdk for error tracking
# TODO: Add redis for conversation/session state if needed
